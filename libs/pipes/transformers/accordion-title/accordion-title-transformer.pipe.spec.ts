import { AccordionTitleTransformerPipe } from './accordion-title-transformer.pipe';

describe('AccordionTitleTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new AccordionTitleTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
