import { JobFilterTransformerPipe } from './jobFilter-transformer.pipe';

describe('JobFilterTransformerPipe', () => {
  it('create an instance', () => {
    const pipe = new JobFilterTransformerPipe();
    expect(pipe).toBeTruthy();
  });
});
